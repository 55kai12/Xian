# 统一换行符，避免 Windows 上出现整文件级别的假改动
* text=auto eol=lf

# Windows 脚本必须保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.apk binary
*.jar binary
*.keystore binary
*.jks binary
