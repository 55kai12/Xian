<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 锁机页的「清空后台应用」：普通权限，声明即得（效果见 LockMachineOverlayController） -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 定时锁机与到期提醒：Android 12 起 setAlarmClock 必须持有本权限，缺了会抛异常。
         Android 14 起不再默认授予，用户需在「闹钟和提醒」里手动开；
         拿不到时代码会自动退化成不精确闹钟（见 ExactAlarms），功能不会失效。 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- 应用限额的前台应用识别：读系统使用记录（数字健康用的同一份），
         比无障碍窗口事件可靠得多 —— 进程被冻结/清掉也能补算。
         这是特殊权限，声明后还需用户在「使用情况访问」里手动授予。 -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".FocusApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:allowBackup="false"
        android:theme="@style/Theme.PomodoroFocus">
        <!-- excludeFromRecents：不进最近任务列表 ⇒ 划不掉、也进不了「一键清理」的名单，
             而划掉正是国产 ROM 清进程的触发点（见 GuardService 的说明）。 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.FocusTimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Pomodoro countdown and completion reminder" />
        </service>

        <service
            android:name=".LockMachineService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Custom app lock with whitelist and countdown" />
        </service>

        <service
            android:name=".GuardService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keep app limits and scheduled lock running after the task is swiped away" />
        </service>

        <service
            android:name=".FocusLockAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service" />
        </service>

        <!-- 开机 / 快速开机 / 覆盖安装后恢复锁机计划、到期提醒与健康体检。
             少了 QUICKBOOT_POWERON 这条，部分国产 ROM 重启后收不到任何广播。 -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".LockAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.xian.focus.action.LOCK_START" />
                <action android:name="com.xian.focus.action.LOCK_END" />
            </intent-filter>
        </receiver>

        <!-- 任务 / 倒数日到期提醒。显式 Intent 触发，不需要 intent-filter，也不需要 exported。 -->
        <receiver
            android:name=".ReminderReceiver"
            android:exported="false" />

        <!-- 锁机掉线自检，同样由显式 Intent 触发。 -->
        <receiver
            android:name=".LockHealthCheckReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
